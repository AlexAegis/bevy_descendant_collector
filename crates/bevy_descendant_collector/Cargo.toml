[package]
name = "bevy_descendant_collector"
description = "Bevy crate to collect named entities into a single component"
version = "0.1.0"
edition = "2021"

homepage.workspace = true
repository.workspace = true
documentation.workspace = true
authors.workspace = true
license.workspace = true

[lints]
workspace = true

[features]
dev = ["bevy/dynamic_linking"]

[dependencies]
bevy = { version = "0.13" }
bevy_descendant_collector_derive = { version = "0.1.0", path = "../bevy_descendant_collector_derive" }

[dev-dependencies]
bevy_asset_loader = { version = "0.20.0" }
bevy-inspector-egui = { version = "0.23.4" }
