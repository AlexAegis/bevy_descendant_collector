[package]
name = "bevy_descendant_collector"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[features]
dev = ["bevy/dynamic_linking"]

[dependencies]
bevy = { version = "0.13.0" }
bevy_descendant_collector_derive = { path = "../bevy_descendant_collector_derive" }

[dev-dependencies]
bevy_asset_loader = { version = "0.20.0" }
bevy-inspector-egui = { version = "0.23.2" }
