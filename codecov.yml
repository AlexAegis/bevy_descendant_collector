component_management:
  default_rules:
    statuses:
      - type: project
        target: auto
  individual_components:
    - component_id: bevy_descendant_collector
      name: bevy_descendant_collector
      paths:
        - crate/bevy_descendant_collector/**
    - component_id: bevy_descendant_collector_derive
      name: bevy_descendant_collector_derive
      paths:
        - crate/bevy_descendant_collector_derive/**
